{
  "name": "css-manifest-require-hook",
  "version": "0.1.0",
  "description": "Require hook to load CSS Modules JSON manifest for server-side rendering",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@diffcunha",
  "license": "MIT",
  "dependencies": {
    "parent-package-json": "^2.0.0"
  }
}
